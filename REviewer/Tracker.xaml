<Window x:Class="REviewer.Tracker"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Enemy Tracker" Height="auto" Width="{Binding WindowWidth, Mode=TwoWay}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        SizeToContent="Height"
        Background="#1E1E1E"
        WindowStyle="None"
        AllowsTransparency="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border BorderBrush="{StaticResource Brush.Border.Subtle}" BorderThickness="1">
        <Grid Height="{Binding WindowHeight, Mode=TwoWay}">
            <Grid.LayoutTransform>
                <ScaleTransform ScaleX="{Binding WindowScale}" ScaleY="{Binding WindowScale}"/>
            </Grid.LayoutTransform>

            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>  <!-- Header -->
                <RowDefinition Height="*"/>   <!-- Enemy List -->
            </Grid.RowDefinitions>

            <!-- Header Bar -->
            <Border Grid.Row="0" Style="{StaticResource WindowHeaderBar}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="ENEMY TRACKER" Style="{StaticResource WindowHeaderTitle}"/>
                    
                    <!-- Position Toggle Button (Above/Below) -->
                    <Button Grid.Column="1" x:Name="PositionBtn" Style="{StaticResource DockButton}" 
                            Click="PositionButton_Click" ToolTip="Toggle Position (Above/Below SRT)">
                        <Path Data="M4,0 L4,8 M0,4 L8,4" 
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" 
                              StrokeThickness="1.5" Width="10" Height="10"/>
                    </Button>
                    
                    <!-- Dock Button -->
                    <Button Grid.Column="2" x:Name="DockBtn" Style="{StaticResource DockButton}" 
                            Click="DockButton_Click" ToolTip="Dock with SRT">
                        <Path Data="M3,3 L13,3 L13,13 L3,13 Z M5,0 L15,0 L15,10 L13,10 L13,3 L5,3 Z" 
                              Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" 
                              Width="16" Height="16" Stretch="Uniform"/>
                    </Button>
                    
                    <!-- Close Button -->
                    <Button Grid.Column="3" Style="{StaticResource DockButton}" Click="CloseButton_Click">
                        <Path Data="M0,0 L8,8 M8,0 L0,8" 
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" 
                              StrokeThickness="1.5" Width="10" Height="10"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Enemy List -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" 
                          Background="{StaticResource Brush.Background.Dark}">
                <ItemsControl ItemsSource="{Binding Tracking}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource EnemyRow}" 
                                    Background="{Binding Enemy.BackgroundColor}" 
                                    Visibility="{Binding Enemy.Visibility}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="70"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="90"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Enemy Name -->
                                    <TextBlock Grid.Column="0" 
                                               Text="{Binding Enemy.Name}" 
                                               Style="{StaticResource EnemyName}"/>

                                    <!-- Health Bar -->
                                    <ProgressBar Grid.Column="1" 
                                                 Value="{Binding Enemy.CurrentHealth}" 
                                                 Maximum="{Binding Enemy.MaxHealth}" 
                                                 Style="{StaticResource EnemyHealthBar}"
                                                 Margin="8,0"/>

                                    <!-- HP Values -->
                                    <TextBlock Grid.Column="2" Style="{StaticResource EnemyHP}">
                                        <Run Text="{Binding Enemy.CurrentHealth}"/><Run Text="/"/><Run Text="{Binding Enemy.MaxHealth}"/>
                                    </TextBlock>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>